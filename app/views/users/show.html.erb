<div class="row">
	<div class="col-xs-3"><h2>User info</h2>
			<%= render 'users/profile', user: @user %>
			<!--同コントローラーのviewで表示ならusersは省略可 -->

			<h2>New book</h2>
			<%= render 'books/newform', book: @book %>
	</div>

	<div class="col-xs-9">
			<h2>Books</h2>
			<!--books一覧 -->
			<%= render partial: 'book_index', locals: {books: @user.books} %>
			<!--books一覧 -->
			<!-- お気に入り一覧 -->
			<h2>Favorite Books</h2>
			<%= render partial: 'book_index', locals: {books: @user.favorite_books} %>
			<!-- お気に入り一覧 -->
	</div>
</div>